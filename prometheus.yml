global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'doktriai'
    metrics_path: '/api/metrics' # Tells Prometheus to target /api/metrics instead of /metrics
    static_configs:
      - targets: ['host.docker.internal:18080']
